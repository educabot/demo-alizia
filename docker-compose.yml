services:
  postgres:
    image: postgres:16-alpine
    container_name: av3-postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: av3
    ports:
      - "5480:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: